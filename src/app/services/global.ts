// Configuration that we will have in several files
// We create this variable to use it in different services and not have to enter the URL multiple times
export var Global = {
    url: 'http://localhost:3700/api/'
};